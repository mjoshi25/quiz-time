import{Outlet,Link,useNavigate}from"react-router-dom";import{useEffect,useState}from"react";import{api}from"../api/apiClient";import{useAuth}from"../context/AuthContext";import Sidebar from"../components/Sidebar";import Brand from"../components/Brand";export default function DashboardLayout(){const{user,logout}=useAuth();const nav=useNavigate();const[count,setCount]=useState(0);useEffect(()=>{let active=true;const load=()=>api.unreadNotificationCount().then(d=>{if(active)setCount(d?.count||0)}).catch(()=>{});load();const timer=setInterval(load,10000);return()=>{active=false;clearInterval(timer)}},[]);return <div className="dashboard-shell"><Sidebar/><section className="dashboard-main"><header className="dashboard-topbar"><Brand compact /><div className="top-actions"><Link className="notification-top-link" to="/notifications" title="Notifications">🔔{count>0&&<b>{count>99?"99+":count}</b>}</Link><span>Welcome, {user?.name}</span><button className="icon-button" onClick={()=>{logout();nav("/")}}>↪</button></div></header><main className="dashboard-content"><Outlet/></main></section></div>}